// Set this after deploying the backend, e.g. "https://portfolio-api.example.com/api".
// Leaving it blank keeps local/offline fallback behavior for development.
window.PHOTO_API_URL = window.PHOTO_API_URL || "";
