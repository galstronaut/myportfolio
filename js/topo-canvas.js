/**
 * TOPOGRAPHIC CANVAS (DISABLED FOR PURE CLEAN / POLOS BACKGROUND)
 */
(function() {
  const canvas = document.getElementById("topo-canvas");
  if (canvas) {
    canvas.style.display = "none";
  }
})();